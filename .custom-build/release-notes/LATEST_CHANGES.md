### 2026-08-24 上游同步与手动构建

- 同步 `upstream/main` 至 `714130a3d746`；保留 Bettbox 自定义更新源、托盘图标、网络面板及现有自定义构建逻辑。
- `custom-build.yml` 移除 `push` 触发器，仅保留 `workflow_dispatch`，后续自定义打包统一手动触发。

### 本次上游融合与测速回退

- 已同步 `upstream/main` 最新代码（当前提交 `433af6a9` / `v1.18.10`），冲突处保留双方不重复能力，重复实现以远端为准。
- 删除自定义测速链路重构及其专用测试，测速恢复 Bettbox 原有 Dart 批处理、桥接超时、Mihomo URLTest 和 Provider 健康检查逻辑；保留 `concurrencyLimit` 配置字段以兼容旧数据，但恢复由上游运行时读取。
- 私有 `custom-mihomo` 已按当前 Bettbox 内核树重新生成并通过 Snell v6 适配测试，避免打包阶段因内核树校验不一致而停止。

### 2026-08-21 更新检查与菜单栏图标修复

- Android 检查更新改用 `custom-update-feed` 分支静态 JSON，不再依赖 GitHub Releases API 的匿名额度；静态源携带最新 Release tag、APK 下载地址、文件大小和 SHA-256，兼容 raw GitHub 的 text/plain 与 gzip 返回内容，继续复用现有 Android 下载、校验和覆盖安装流程。
- 恢复 macOS 菜单栏托盘的模板图标与原生 active/inactive 着色，修复上游同步后活动状态显示纯黑图标的问题；保留速率文字布局修复。

### WebDAV 配置同步切割

- WebDAV 备份改为仅同步“配置”页中的订阅配置、每份配置的覆写规则，以及已添加的 JavaScript 覆写脚本；本地文件备份继续保留完整应用数据。
- 当前选中的配置、当前启用的覆写脚本、节点选择、策略组展开状态、Provider 缓存，以及系统代理、虚拟网卡、主题、窗口、托盘、快捷键和其他平台设置不再写入或恢复自 WebDAV。
- 恢复共享配置时按配置 ID 保留本机节点选择与展开状态；只有本机当前配置已被远端覆盖删除时，才回退到恢复后的第一份配置。旧版完整 WebDAV 备份也按新的共享边界读取，避免再次覆盖本机平台设置。

### 独立网络面板

- 桌面导航继续以独立“面板”替换原“请求 / 连接 / 日志”页签；Android 不再设置“工具 → 面板”二级目录，最近请求、活动连接、DNS、设备、流量统计、日志和 Sub-Store 直接作为“网络”分组的一级入口。
- Android 请求与连接使用纵向卡片惰性列表，点击后压入独立详情路由，系统返回手势只返回列表；DNS、设备、流量和日志使用移动端单列布局，不再复用需要横向滚动的桌面表格。
- Android 请求与连接详情页右上角增加三点菜单，复用桌面端完整的“生成规则”流程，可生成、复制、添加覆写或补充至 Sub-Store，并保留复制详情入口。
- Android 规则生成、Sub-Store 凭据与规则管理弹窗统一使用紧凑字号；策略候选改为可完整滚动的移动端底部列表，修复长策略列表无法选择末尾条目。请求详情顶部只显示目标链接，客户端信息保留在通用详情中。
- macOS/Windows/Linux 独立面板保留顶部页签、搜索和左侧 Mihomo 分类，内容区统一复用 Android 的卡片页面，不再显示旧桌面表格；选中连接仍在底部展开可拖动详情，拖动高度保存为本机偏好，右键继续提供“生成规则”。Android 保持工具目录和独立详情路由。
- Android 与桌面请求/连接均可按进程、来源、目标、协议、规则、出站链和连接状态筛选；Android 其他页面恢复横向分类条，设备、流量和日志分类直接参与数据过滤。选中分类改为透明底色、主题主色描边和明确前景色，避免自定义主题背景与文字同色。
- DNS 页面将配置 DNS、Hosts、运行缓存和 Fake-IP 显式分组，列出面板当前可确认的运行时解析结果与数量；清除内核 DNS/Fake-IP 缓存后同步清空这些可见历史，后续连接重新产生解析结果。移动端和桌面均保留当前页搜索。
- Mihomo 链路固定先显示当前连接的请求目标和目标解析方式；Fake-IP 代理目标会明确显示“真实解析交由代理远端完成”，不再将节点 DNS 冒充为当前目标 DNS。目标域名解析与代理节点服务器解析分别标记为“目标 DNS / 节点 DNS”，完整策略链按“命中规则 → 策略组 → 中间组 → 最终节点”顺序展示，代理连接的 socket 对端明确命名为“代理入口地址”，不再与首页检测到的出口公网 IP 混淆。链路中出现的代理节点 DNS 成功或缓存命中也会进入 DNS 页的“运行缓存 / 出站节点”分类，可直接搜索与清除。
- 同步 Bettbox 上游 `main` 至 `f393ebee`，包含编辑器快捷注释、Emoji 回退及 QuickJS 类型修复。
- 桌面入口改为启动独立网络面板进程，在 Dock/任务栏中使用逐像素保留 Bettbox 原图标、仅于右下角叠加蓝色网络波形徽标的专属图标；关闭面板不影响 Bettbox，Bettbox 退出或崩溃时会连带关闭面板。
- 移除 `desktop_multi_window` 及子引擎全插件重复注册，修复打开网络面板后 `tray_manager` 事件通道被覆盖、托盘左右键无法弹出菜单或主窗口的问题。
- 面板通过现有本地 UDS/TCP 控制通道复用主进程数据和操作，统一展示最近请求、活动连接、DNS、设备、流量统计和日志，不创建第二个 Mihomo 实例。
- 最近请求与活动连接按 Mihomo `TrackerInfo` 的进程、来源地址、目标地址、网络协议、规则类型和出站链动态分类；支持搜索，桌面选中后展开底部详情。macOS 进程分类、连接列表和详情读取所属 App 的原生图标。
- 修复惰性列表复用可见行时短暂沿用上一进程图标、无图标行不恢复默认状态的问题；进程侧栏优先使用 Mihomo 提供的有效 App 路径，macOS 继续直接通过 `NSWorkspace` 读取对应应用图标。
- 删除旧桌面表格、表头排序与列宽持久化实现，Android 和桌面统一维护同一套卡片列表；规则与最终策略使用同一数据映射，完整策略链仅在详情中展示。
- 最近请求与活动连接支持右键“生成规则”，可从当前域名、IP、进程名/路径和端口生成 Clash/Mihomo 规则；规则类型限定下拉选择，匹配内容与策略可手动编辑，策略也可从当前策略组和节点直接替换。生成结果可直接加入当前配置的“附加到原始规则 / 覆盖原始规则”，重复时二次确认但不强制开启覆写；Sub-Store 入口可补充新规则，也可读取 `BETTBOX_CUSTOM_RULES` 后修改、删除和拖动排序，保存时重新读取最新脚本并仅替换该变量。面板在“日志”后增加独立 `Sub-Store` 标签；凭据弹窗按当前内容动态调整高度，loading 沿用弹窗圆角裁剪，读取成功后原位展开管理页，管理页取消回到 Sub-Store 凭据页。管理页每条规则使用带边框的独立卡片，并增加可选说明；保存后在对应规则上方生成 `// 说明：…` JS 注释，拖动、修改和删除不会与其他规则串位。成功使用过的文件地址和 API Key 保存在本机历史，只有输入框右侧箭头会展开选择。
- 策略名在数据入口统一压缩全角空格、不换行空格、Emoji 相邻空格和连续空白，列表、详情、规则弹窗及复制结果共用同一规范化结果；修复内置 HarmonyOS Sans 字体缺少普通空格字形、导致所有使用该字体的文本按 1em 放大词间距的问题，并移除面板内逐处拆分 `TextSpan` 的临时规避逻辑。策略输入区点击只负责编辑，仅右侧箭头展开候选策略。
- 请求详情面板增加纵向拖拽手柄，可上下调整高度；通用页改为占满视口的响应式布局，地址和进程独占整行，移除固定卡片宽度及单行省略，全部详情与 Mihomo 链路文字均可框选复制，滚动条固定在面板最右侧，并提供复制详情按钮。IP 信息区分客户端地址、目标地址、内核 Fake-IP、实际出站地址和直连目标/代理节点远端地址，GeoIP/ASN 仅在内核或现有 GeoIP 查询能够返回时显示。
- 详情页移除 Mihomo 不支持的 HTTP 请求/响应报头与正文页签；“计时 & 日志”改为“Mihomo 链路”，优先展示 custom-mihomo 随当前连接返回的 DNS 尝试、规则匹配、策略链和真实 socket 建立事件，不再混入同进程的其他请求日志。
- 修复面板打开时同一进程图标被大量并发读取、macOS 主线程反复编码全尺寸 PNG，导致面板 CPU、内存暴涨并卡死的问题；图标请求按进程合并，原生端固定输出 64×64 PNG。
- 修正顶栏搜索框对齐；设备页仅按内核可确认的本机进程、网络来源及活动/历史状态分类，不再伪造静态 IP、网关或接管模式；日志只按实际事件的 `error / warning / info / debug` 级别筛选。
- 请求与连接列表移除无意义的 ID 展示；状态改为红色失败/拦截、黄色建立中、蓝色已连接、绿色已结束和灰色未知，依据 Mihomo 活动快照、真实出站 socket、`REJECT` 与链路终态判断。“日期”列改为“时间”并按本机时区显示，策略名称中的连续空白统一压缩。
- 请求、连接、DNS 和设备表改为固定行高的惰性列表，只创建可见行并复用滚出视口的元素；实时事件最多每 250 ms 刷新一次，避免记录增长后整表重建造成持续卡顿。水平、垂直滚动保持独立并裁剪到列表区域。
- DNS 按 Mihomo 配置键 `default-nameserver / nameserver / fallback / proxy-server-nameserver / direct-nameserver / nameserver-policy / hosts`、系统 `/etc/hosts` 和运行时 `dnsMode` 分类；运行时记录仅保留带有效目标 IP 的域名。
- DNS 页增加“清除 DNS 缓存”，同时清理内核 DNS 结果与 Fake-IP 映射；Mihomo 链路对缓存命中明确标记“本次未发起 DNS 查询”，步骤标题列按实际最长文案调整宽度。
- Mihomo 链路将时间戳与事件内容拆成独立列，长 DNS 结果换行后仍从内容列起始位置对齐，不再与时间戳上下重叠。
- 流量页直接显示 Mihomo 的实时与累计上传/下载，连接记录只用于按出站链、规则类型、进程、来源地址、网络协议和目标主机聚合明细，不再把有限历史记录求和冒充内核总流量。
- 无进程、无来源地址的 Mihomo 内部连接不再污染请求与连接列表；具有真实客户端的 `REJECT` 规则命中继续保留并显示红色状态点。
- 请求和日志到达后实时推送；活动连接在连接页以 250 ms 快照增量更新，并复用主进程核心，不创建第二个 Mihomo 实例。
- Mihomo 在连接加入和离开时均回传同一连接 ID，请求记录按 ID 更新，最近请求页由事件立即刷新活动/完成状态，不再进行 250 ms 主动快照；订阅推送显式刷新 socket，避免退化为 5 秒兜底刷新。浏览器复用同一 HTTP/2 或 QUIC 连接时不会伪造新的 HTTPS 请求记录。
- 修复 macOS 首屏部分进程图标保持占位、滚出再滚回才显示的问题：缺少进程路径时由 `NSWorkspace` 按运行中 App 名称读取，同进程记录复用快照中的有效 `.app` 路径；图标加载完成回调不再返回并自等待当前 Future，当前可见组件会立即原位替换占位图，无需依赖滚动重建。
- 独立面板不记录、不恢复也不主动激活 Bettbox 或其他应用；除主进程负责其生命周期外，Dock、Cmd+Tab 和关闭窗口后的前台选择完全交给 macOS，表现为两个互不关联的普通 App。
- 主进程处理完网络面板的一次性 UDS/TCP 请求后立即刷新响应并关闭服务端 socket，订阅连接仍保持长连，避免反复刷新面板持续泄漏文件描述符。
- HTTP 报头、正文、流量捕获和 MitM 不伪造，网络面板不展示 Mihomo 没有提供的数据入口。

### 托盘与代理开关

- 托盘菜单将系统代理置于虚拟网卡上方；“显示窗口 / 网络面板 / 系统代理 / 虚拟网卡 / 重启内核 / 退出”分别显示 `⌘M / ⌘D / ⌘S / ⌘E / ⌘R / ⌘Q`，重启内核移到一级菜单，重启软件移入工具子菜单。
- 系统代理与虚拟网卡均关闭时，托盘上传、下载立即归零并保持灰色；托盘改为监听内核实际 TUN 状态，重新开启系统代理或虚拟网卡完成后强制同步最终状态，图标与网速恢复系统高亮色，不再停留在启动过程中的灰色快照。
- Bettbox 仅关闭系统代理开关时不再主动关闭 macOS、Linux 或 Windows 当前系统代理；只有本进程成功启用过系统代理后才负责关闭，确保系统代理与虚拟网卡均关闭时 Bettbox 不参与系统网络设置。
- 自定义构建 Action 支持仅构建并发布 macOS Apple Silicon 或 Android arm64-v8a；单平台构建不会启动其他平台任务，两个选项不能同时启用；包含 Android 时通过独立 worktree 同步静态 Android 更新源，桌面更新源仍按构建平台生成。

### 内网接口 DNS

- Mihomo direct proxy 新增 `dns-follow-interface`，绑定 en5 的内网直连代理会自动使用 en5 当前 DNS 解析目标域名。
- 新增默认开启的 `allow-other-interface`；en5 不存在、未运行或没有可用地址时，连接和 DNS 自动切到当前默认接口，插入内网 Wi-Fi 后无需手动改为普通 DIRECT。
- macOS 优先读取系统已有的 en5 作用域 DNS；正常状态不轮询，解析失败或网络重置时才刷新并重试。
- 覆写脚本不再为内网域名重复维护 `nameserver-policy`，固定 hosts 仍保留为注释回退。
- 覆写脚本将无条件固定规则集中到 JSON 字符串数组 `BETTBOX_CUSTOM_RULES`，CC 内网、抓包和 Emby 的条件规则集中由 `buildCustomRules()` 生成，为网络面板安全管理固定规则提供唯一稳定入口。

### 界面调整

- “增强工具”中的“托盘点击行为”和“显示托盘网速”恢复为两条独立设置，不再合并在同一卡片中。
- Android 首页总开关避让悬浮底部导航，页面底部同时增加完整的按钮滚动留白，避免按钮被遮挡或覆盖仪表盘内容。

### TUN 与系统 DNS

- macOS 将会导致微信聊天图片/表情一直 Loading 的 Mihomo `system` TUN 栈统一映射为 `mixed`；用户显式选择 `mixed` 或 `gvisor` 时保持原值。
- 桌面端切换 TUN 栈时完整重启内核，不再热重建运行中的 TUN；失效的核心 IPC socket 会立即清除，内核异常退出时记录真实退出码和 stderr，避免弹出次生 `Broken pipe`。
- 移除“自动设置系统 DNS”开关；macOS 开启 TUN 时自动托管系统 DNS，关闭 TUN 或停止应用时自动恢复。
- 旧配置中的开关字段不再生效，避免关闭 DNS 托管后出现 TUN 流量异常。
- macOS 启动 TUN 前检测其他 VPN 遗留的 `1.0.0.0/8` 路由；发现冲突时保持 TUN 关闭并提示对应接口，避免直接启动失败。
- 修复 macOS 关闭旧 TUN 时 `ENOTSOCK` 未被识别为关闭，导致 `batch read packet` 无限刷屏、核心与界面 CPU 满载的问题；关闭感知包装器保留 Mixed/GVisor 方法集，修复选择 `mixed` 后核心崩溃、界面继而报 `Broken pipe` 且无法联网的问题。

### 节点测速

- Provider、Fallback、策略组和手动测速全部取消并发上限；Dart、桥接和 Mihomo 直接并行提交所有项目，移除核心 16 并发管理器与 Provider 10 并发限制。
- 删除全链路节点去重、在途合并、前置排队、防抖和结果缓存；重复节点也会独立发起测速，不再共享其他入口的成功或失败结果。桥接旧 `mBatch` 结果泄漏同时移除。
- 核心测速超时调整为 10 秒，桥接等待为 12 秒；解析失败、节点不存在或其他异常都会结束加载并显示检测失败，避免秒出超时、永久转圈和结果跳变。
- Provider 后台健康检查失败不再覆盖页面已有测速结果；只有用户主动测速才写入失败终态。
- 覆写脚本使用全局 `latencyTestUrl` 统一 OwO、源 Provider、Inline Provider 和策略组测速地址，当前值为 `https://g.cn/generate_204`。
- Bettbox 开启“覆写测速链接”时，同时覆盖策略组和 Provider 健康检查地址。
- 增加节点测速全链路诊断日志，以 request ID 串联 Dart、桥接和 Mihomo 网络阶段，并记录 Provider batch ID、失败阶段、context、错误类型及各段耗时；不改变测速、并发、超时或重试行为。
- 应用内日志与请求记录容量从 256 提高到 1024 条，完整保留一轮约 90 节点的详细测速记录。
- 配置重载时立即关闭旧 Proxy Provider，取消上一代健康检查，修复首次启动或覆盖安装后同一 Provider 出现两批测速的问题。
- 每次测速拥有独立 context 与超时，不存在队列等待时间；手动测速使用 10 秒网络超时，Provider 使用自身配置超时，调用方已有更短 deadline 时保持调用方限制。
- Provider 整批健康检查不再 singleflight 合并；配置重载或 Provider 更新仍关闭旧 Provider 并阻止旧节点发布过期结果，新节点立即独立检查。成功、失败均不缓存，也不增加重试。
- 用户显式使用 HTTP 测速时只发送一次标准 GET，避免部分线路随机断开 HEAD；不增加失败重试。

### 上游同步

- 移除 `custom-sync.yml` 每天四次的定时触发，仅保留手动同步入口。
- 同步 Bettbox 上游 `main` 的编辑器优化、README 整理和版本号更新。
- 同步 AIsouler 覆写脚本 `7ca7733b` 中与当前结构有关的节点过滤边界、DNS 大小写匹配及 `#DIRECT` 后缀处理；`private_ip` 继续保持上游调整后的尾部直连顺序。
- 同步 Bettbox 上游 `main@6291ab3d7e2d`，版本更新至 `1.19.0+2026081601`；保留 custom-build 的网络面板、TUN/DNS、托盘和应用内更新能力，并修正合并后的配置更新并发、仪表盘生命周期和重复托盘弹窗问题。
- 修复 `lib/manager/tray_manager.dart` 中左右键托盘回调重复声明导致四个平台构建失败的问题。

### 自定义改动维护

- 补全 Bettbox、custom-mihomo 和私有覆写脚本的自定义功能总账、代码落点及回归检查；覆写脚本完整版本进入私有 custom-mihomo 仓库管理。
- 已通过 Sub-Store 文件 API 同步远端 `fx.js`，并逐字节校验为本地 `scripts/uclort-desktop.js` 的最新内容。
- 新增 Sub-Store `uclort-sukka.js` 覆写脚本，以现有 Surge 配置为基准生成 Mihomo 可等价实现的通用、DNS、代理、策略组、跳过代理和规则配置，并接入 SukkaW 官方 Mihomo 规则；源 hosts 只用于替换节点域名，不填充默认 hosts。CC 内网保持独立可关闭开关。修正 Sub-Store 文件来源元数据为本地文件，避免无远程 URL 导致导入接口返回 500；远端脚本通过 Bettbox 实际导入 URL 与私有 custom-mihomo 本地文件逐字节校验。
- 自定义应用代码发生变化时，Actions 同时校验增量说明和完整改动总账均已更新。
