### 更新

- 构建基线升级至上游 `v1.18.9-pre1`，同步该版本包含的应用、内核引用及平台构建调整。

### 修复

- 合并上游新增的隐藏策略组与 macOS 托盘测速实现时，清理重复的配置字段、菜单入口和测速协调器，避免生成代码冲突、菜单重复及状态监听分裂。
- 托盘菜单在保持打开状态时改为按菜单项 ID 更新，避免隐藏项数量或策略组结构变化后按下标更新到错误条目。
- 自动同步改为保留上游 Release 的真实合并历史；以后只合并新增提交，不再因 squash 后缺少祖先关系而重复套用旧版本改动。

### 保留

- 继续保留自定义托盘速率、左右键独立行为、Option 临时显示隐藏项，以及菜单与面板共享测速状态等增强功能。
